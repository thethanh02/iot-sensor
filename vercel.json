{
  "buildCommand": "cd fe && yarn && yarn build",
  "outputDirectory": "fe/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/app.js"
    }
  ]
}